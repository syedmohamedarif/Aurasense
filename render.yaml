services:
  - type: web
    name: aurasense-backend
    env: docker
    dockerfilePath: ./Dockerfile.backend
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: YOUTUBE_API_KEY
        sync: false
